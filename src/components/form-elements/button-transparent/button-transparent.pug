mixin buttonTransparent(options)
  if options.sizeSmall
    a.button-transparent(href=options.href class="button-transparent_size_small")=options.text
  else
    a.button-transparent(href=options.href)=options.text